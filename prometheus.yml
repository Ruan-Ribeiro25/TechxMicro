global:
  scrape_interval: 15s # Coletar dados a cada 15 segundos

scrape_configs:
  - job_name: 'vidaplus-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # ATENÇÃO: 'app' é o nome do serviço no docker-compose (veja o passo 3)
      - targets: ['app:8080']